import styled from 'styled-components';


const ImageShowStyle = styled.div`
    div {
        flex: 1 1 auto;
    }
    img {
        display: block;
        max-width: 100%;
        width: auto;
        height: 300px;
        object-fit: cover;
        cursor: pointer;
        margin: 0;
        padding: 0;
    }
`

export default ImageShowStyle;
